# action.yml
name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  myInput:  # id of input
    description: 'Who to greet'
    required: true
outputs:
  tag_new: # id of output
    description: 'Test output'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - 'Mona the Octocat'